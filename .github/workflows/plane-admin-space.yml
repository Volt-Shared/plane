name: deploy-admin-space

on:
  workflow_dispatch:
  push:
    branches: [ master, preview ]
    paths:
      - 'apps/admin/**'
      - 'apps/space/**'
      - 'k8s/14-plane-admin-space.yaml'
      - '.github/workflows/plane-admin-space.yml'

jobs:
  build-push-sync:
    runs-on: ubuntu-latest
    permissions: { contents: write, packages: write }
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.volt_infra_pat }}

      - name: login to ghcr
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.volt_infra_pat }}

      - name: build admin
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./apps/admin/Dockerfile.admin
          push: true
          tags: ghcr.io/volt-shared/volt-plane-admin:latest

      - name: build space
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./apps/space/Dockerfile.space
          push: true
          tags: ghcr.io/volt-shared/volt-plane-space:latest

      - name: git commit
        run: |
          git config user.name "volt-bot"
          git config user.email "actions@github.com"
          git add k8s/14-plane-admin-space.yaml
          git commit -m "chore(ops): sync admin-space [skip ci]" || echo "No changes"
          git push origin ${{ github.ref_name }}